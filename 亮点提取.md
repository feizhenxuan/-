# 核心亮点提取 — 解决问题的思考过程

> 这份文档不讲代码细节，而是讲**为什么这么做**——每个设计决策背后要解决的问题是什么。

---

## 亮点一：会话持久化 + 槽位管理

### 遇到的问题

- 用户对话到一半，服务重启了，之前的对话全丢了
- 用户分多轮提供信息（先说电影名，再说时间，再说影院），后端需要记住之前收集的信息
- 不同用户不能看到彼此的对话

### 思考过程

参考 Web 应用的 Session 管理机制，把对话状态持久化到 MySQL：

| 数据 | 存什么 | 为什么 |
|------|--------|--------|
| slots | 电影名、影院名、时间、数量 | 多轮对话逐步收集，不能丢 |
| messages | 对话历史 | 服务重启后恢复上下文 |
| lastIntent | 最后一次意图 | 判断用户是否在继续之前的话题 |
| gmtExpire | 过期时间（60 分钟） | 超时自动创建新会话 |

### 关键设计决策

**1. 槽位合并写入（不是覆盖）**

问题：用户第一轮说"我想看教父"（槽位：movie=教父），第二轮说"明天下午"（槽位：time=明天下午）。如果直接覆盖，电影名就丢了。

方案：`mergeSlots()` 先读已有槽位再 merge，不是覆盖。

```java
// ChatAgentService.java
private void mergeSlots(ChatSession session, Map<String, Object> extracted, String city) {
    Map<String, Object> existing = parseJson(session.getSlots(), Map.class);
    // 覆盖语义：movie_name 变 → 清 cinema_name（因为影院和电影是关联的）
    if (extracted.containsKey("movie_name") && !Objects.equals(
            extracted.get("movie_name"), existing.get("movie_name"))) {
        existing.remove("cinema_name");
        existing.remove("session_id");
    }
    existing.putAll(extracted);  // 合并，不是替换
    session.setSlots(MAPPER.writeValueAsString(existing));
}
```

**2. 电影名变了自动清空影院名**

问题：用户先选了"教父+万达影院"，后来改说"我想看流浪地球"。如果不清空影院名，系统会用"流浪地球+万达影院"去查场次，但万达可能没有流浪地球的排片。

方案：`movie_name` 变化时自动清空 `cinema_name` 和 `session_id`（派生槽位）。

**3. 消息历史截断**

问题：对话越来越长，token 不够用。

方案：最多保留最近 20 条消息（约 10 轮对话），超出部分直接截断。

```java
int maxHistory = 20;
int startIndex = Math.max(0, history.size() - maxHistory);
List<Map<String, Object>> trimmedHistory = history.subList(startIndex, history.size());
```

**4. 会话归属校验**

问题：用户 A 不能看到用户 B 的对话。

方案：`getOrCreateSession()` 校验会话归属，已存在的 session 必须属于当前用户。

```java
if (session.getUserId() != null && userId != null
        && !session.getUserId().equals(userId.intValue())) {
    return null;  // 不是自己的会话，拒绝访问
}
```

---

## 亮点二：正则优先 + LLM 兜底的意图识别

### 遇到的问题

全部走 LLM 意图识别：
- 每次调用消耗 token，成本高
- LLM 响应有延迟（1-3 秒），用户体验差
- LLM 返回结果不稳定

### 思考过程

最初的想法是：高频意图用正则直接匹配，不调 LLM，毫秒级响应。

但实际开发中发现：**正则覆盖的场景非常有限**。用户表达意图的方式千变万化：
- "我想看教父" → 正则能匹配"想看"
- "教父好看吗" → 正则匹配不到
- "明天下午去看电影" → 正则可能匹配不到
- "附近有什么影院" → 正则能匹配"影院"
- "帮我订两张明天下午的票" → 正则能匹配"订"

大多数情况下，正则匹配不到或匹配不准确，**LLM 兜底才是常态**。

于是设计了两层策略：

```
第1层：正则匹配（不调LLM，毫秒级）
  → 只覆盖明确关键词场景（"想看""买票""退票"等）
  ↓ 正则不够自信（大多数情况）
第2层：LLM 兜底（Function Calling）
  → 处理复杂意图，准确但慢
```

### 关键设计决策

**1. 正则优先，但覆盖有限**

问题：LLM 调用有延迟，高频意图每次都调 LLM 太慢。

方案：把"想看""买票""退票"等明确关键词做成正则匹配，不调 LLM，毫秒级响应。

```java
// IntentDetector.java
private static final Pattern BOOK_PATTERN = Pattern.compile(
    "(买票|订票|购票|想看|要看|《.+》.*(?:买|订|看)|(?:想去|要去|去).+(?:影院|影城|电影院))"
);
```

**但实际情况是**：正则只能覆盖明确关键词场景，大多数用户表达都不在正则覆盖范围内。

**2. LLM 兜底是常态**

问题：什么时候需要调 LLM？

方案：三个条件触发 LLM 兜底，**大多数情况下都会触发**：
- 意图不明确（正则匹配结果不够自信）
- 消息明显包含信息但正则抽不到任何槽位
- 消息含绝对日期但正则没抽到时间槽位

```java
// ChatAgentService.java
if (!isIntentConfident(content, intent) && !isGreeting(content)) {
    needLLM = true;
}
if (!needLLM && extracted.isEmpty() && content.length() > 5) {
    needLLM = true;
}
if (!needLLM && containsAbsoluteDate(content) && !extracted.containsKey("time_expression")) {
    needLLM = true;
}
```

**3. LLM 只补充不覆盖**

问题：LLM 可能返回和正则不同的结果，听谁的？

方案：LLM 意图只在正则不够自信时覆盖，LLM 槽位只补充正则没抽到的，不覆盖正则已有的。

```java
// LLM 意图只在正则不够自信时覆盖
if (llmResult.intent != null && !isIntentConfident(content, intent)) {
    intent = llmResult.intent;
}
// LLM 槽位只补充正则没抽到的
for (Map.Entry<String, Object> entry : llmResult.slots.entrySet()) {
    if (!extracted.containsKey(entry.getKey())) {
        extracted.put(entry.getKey(), entry.getValue());
    }
}
```

**4. 意图优先级**

问题：多个意图同时匹配时，听谁的？

方案：按优先级排序：打招呼 > 退票 > 查订单 > 查电影 > 购票 > 查影院。

```java
// 打招呼最优先
if (GREETING_PATTERN.matcher(text).find()) return Intent.REJECT;
// 退票优先
if (REFUND_PATTERN.matcher(text).find()) return Intent.REFUND_TICKET;
// 查订单在购票之前（避免"我的票"被误判为购票）
if (ORDER_PATTERN.matcher(text).find()) return Intent.QUERY_ORDER;
```

---

## 亮点三：真流式 SSE 输出

### 遇到的问题

需要实时接收 LLM 的回复，但：
- LLM 生成回复需要时间（几秒到十几秒）
- 如果等 LLM 生成完再返回，用户会等很久
- 如果一次性返回，用户没有"正在回复"的感觉

### 思考过程

使用 Spring AI 的 `chatModel.stream()` 实现真流式输出：LLM 每生成一个 token 就立即推送给前端，而不是等全部生成完再返回。

### 关键设计决策

**1. 真流式 vs 模拟流式**

问题：真流式需要 LLM 网关支持 `stream=True`，模拟流式是先拿完整结果再分段推送。

方案：SOFA AI 网关支持真流式，直接用 `chatModel.stream()` 逐 token 推送。

```java
chatModel.stream(prompt)
    .doOnNext(chunk -> {
        String delta = chunk.getResult().getOutput().getText();
        if (delta != null && !delta.isEmpty()) {
            // 逐 token 推送给前端
            eventCallback.accept(Map.of("type", "text_delta", "content", delta));
        }
    })
    .blockLast();
```

**2. Agent 循环（ReAct）**

问题：LLM 可能需要调用工具（查电影/查影院/购票），工具执行完还需要继续生成回复。

方案：手写 ReAct 循环，最多 5 轮迭代：

```java
for (int iteration = 0; iteration < MAX_ITERATIONS; iteration++) {
    // 调用 LLM
    chatModel.stream(prompt).doOnNext(chunk -> { ... }).blockLast();

    if (!tcBuilders.isEmpty()) {
        // 有工具调用：执行工具，回灌 messages，继续循环
        for (ToolCallBuilder tcb : tcBuilders.values()) {
            String result = callback.call(toolArgs);
            toolResponses.add(new ToolResponseMessage.ToolResponse(...));
        }
        messages.add(new ToolResponseMessage(toolResponses));
        continue;  // 继续下一轮迭代
    }

    // 没有工具调用：纯文本回复，结束循环
    break;
}
```

**3. 工具调用参数自动注入**

问题：LLM 调用工具时可能缺少必要参数（如用户定位、用户 ID）。

方案：后端自动注入：
- `search_sessions` / `search_cinemas`：注入用户经纬度
- `search_sessions`：注入槽位中的时间参数
- `get_user_orders` / `refund_order` / `cancel_order`：强制注入当前登录用户 ID

```java
if (("search_sessions".equals(toolName) || "search_cinemas".equals(toolName))
        && lat != null && lng != null) {
    toolArgs = injectLocationIntoArgs(toolArgs, lat, lng);
}
if ("search_sessions".equals(toolName)) {
    toolArgs = injectTimeSlotsIntoArgs(toolArgs, session);
}
if (("get_user_orders".equals(toolName) || "refund_order".equals(toolName)) && userId != null) {
    toolArgs = injectUserIdIntoArgs(toolArgs, userId);
}
```

**4. 卡片合并去重**

问题：LLM 可能同时调用 `search_movies` 和 `recommend_movies`，产生多张重复的电影卡片。

方案：对相同 `card_type` 的卡片做合并去重，按 item 的 id 字段去重。

---

## 亮点四：打字机效果

### 遇到的问题

后端 SSE 推过来的文本如果直接显示，会出现两种问题：
- 推得太快：一大段文字瞬间出现，用户没有"正在回复"的感觉
- 推得太慢：一个字一个字蹦，用户等得不耐烦

### 思考过程

观察 ChatGPT 的打字机效果：不是逐字显示，而是**每次显示 2-3 个字符**，速度自然流畅。

于是设计了一个 buffer + 定时器的方案：
1. 后端推过来的文本先存到 `typingBuffer`
2. 用 `setInterval` 每 20ms 从 buffer 取 2-3 个字符
3. 追加到最后一条消息上，触发 React 重渲染

### 关键设计决策

**1. 每次取 2-3 个字符**

问题：取 1 个太慢，取太多又不像打字。

方案：每次取 2-3 个字符，在视觉上最接近自然打字速度。

**2. flushTyping() 确保吐完再回调**

问题：SSE 流结束时，buffer 里可能还有没显示完的文字。如果直接设置 loading=false，用户会看到文字还没显示完 loading 就消失了。

方案：`flushTyping()` 把回调加入队列，等打字机吐完 buffer 再执行。

**3. 切换会话时清空 buffer**

问题：用户正在打字机效果中切换会话，旧会话的文字可能继续在新会话中显示。

方案：`clearMessages()` 和 `loadHistory()` 都会清空 typingBuffer 和定时器。

---

## 亮点五：SSE 流式消费

### 遇到的问题

需要实时接收后端推送的数据，但：
- EventSource 只支持 GET，我们需要 POST 传 JSON body
- 网络数据可能不完整到达（半包），直接解析会报错
- 用户可能中途取消请求

### 思考过程

**为什么用 fetch 不用 EventSource？**

EventSource 是浏览器原生的 SSE 客户端，但只支持 GET 请求。我们的接口需要 POST 传 JSON body（包含消息内容、会话 ID、位置信息），所以用 fetch + ReadableStream。

**半包问题怎么解决？**

网络数据是流式到达的，可能一个 SSE 事件被拆成两次到达。用 buffer 暂存不完整的数据，按 `\n\n` 分割事件，`events.pop()` 保留不完整部分，等下次读取时拼接。

### 关键设计决策

**1. buffer 半包拼接**

```typescript
buffer += decoder.decode(value, { stream: true });
const events = buffer.split('\n\n');
buffer = events.pop() || '';  // 不完整部分留到下次
```

**2. 30 秒超时 + AbortController**

```typescript
timeoutId = setTimeout(() => {
  controller.abort();
  callbacks.onError?.('回复超时，请重试');
}, 30000);
```

**3. 四层错误处理**

- HTTP 状态码错误（401 登录过期）
- 网络连接错误（AbortError 区分用户取消）
- 后端业务错误（data.type === 'error'）
- JSON 解析容错（解析失败静默跳过）

---

## 亮点六：三路并行定位

### 遇到的问题

需要获取用户位置来推荐附近影院，但：
- GPS 定位慢（可能需要几秒），用户等不及
- GPS 定位可能失败（室内、权限拒绝）
- 浏览器定位在 PC 上不可用

### 思考过程

传统方案是串行：先 GPS，失败了再 IP。但这样用户要等 GPS 超时才能看到结果。

我们改成**三路并行**：GPS + IP + 浏览器同时发起，哪个先返回用哪个，5 秒硬超时。

```
三路并行
  ├── 高德 GPS 定位（AMap.Geolocation）
  ├── 高德 IP 定位（AMap.CitySearch）
  └── 浏览器原生定位（navigator.geolocation）
  → 哪个先返回用哪个，5 秒硬超时
```

### 关键设计决策

**1. 三路并行而不是串行**

问题：串行定位慢，GPS 超时后才能试 IP。

方案：三路同时发起，哪个先返回用哪个，大幅提升定位速度。

**2. 5 秒硬超时**

问题：如果三路都失败，不能一直等。

方案：5 秒硬超时，超时后返回 null，由调用方决定兜底逻辑（默认城市）。

**3. 定位失败兜底**

问题：定位失败时不能让用户卡在空白页。

方案：默认城市"郑州东站"，保证应用可用。

---

## 亮点七：登录自动清空聊天记录

### 遇到的问题

用户 A 登录后聊了几句，退出登录。用户 B 登录后，看到了用户 A 的聊天记录。

### 思考过程

需要在登录/登出时清空聊天记录。但登录组件不应该直接操作 chatStore，这样会形成耦合。

### 关键设计决策

**使用 Zustand 的 subscribe 机制解耦**：

```typescript
// authStore.ts
useAuthStore.subscribe((state, prevState) => {
  if (!prevState.token && state.token) {
    // 新 token 出现（登录成功）→ 自动清空聊天记录
    import('./chatStore').then(({ useChatStore }) => {
      useChatStore.getState().clearMessages();
    });
  }
});
```

- 用 `subscribe` 监听 token 变化
- 用动态 `import` 避免循环依赖
- 登录组件不需要关心 chatStore，完全解耦

---

## 亮点八：主题切换水波纹动画

### 遇到的问题

主题切换时页面突然变色，用户体验生硬。

### 思考过程

现代浏览器支持 View Transitions API，可以自动做新旧快照的过渡动画。利用这个 API，可以实现以点击位置为圆心的水波纹扩散效果。

### 关键设计决策

**1. View Transitions API**

```typescript
const vt = document.startViewTransition?.bind(document);
if (vt) {
  const transition = vt(doSwitch);  // 浏览器自动做新旧快照
  transition?.finished?.finally(cleanup);
} else {
  doSwitch();  // 不支持的浏览器直接切换
}
```

**2. 点击坐标写入 CSS 变量**

```typescript
root.style.setProperty('--ripple-x', `${cx}px`);
root.style.setProperty('--ripple-y', `${cy}px`);
```

**3. 降级方案**

不支持 View Transitions API 的浏览器直接切换，不影响功能。

---

## 亮点九：前端组件设计

### 遇到的问题

- 需要区分用户和 AI 的消息
- 需要防止用户重复发送
- 需要支持一键复制 AI 回复
- 需要渲染多种类型的卡片（电影/影院/场次/订单）

### 思考过程

**为什么只用 4 个 antd 组件？**

antd 提供了很多重型组件（Card、List、Spin、Modal），但对话页面的 UI 比较轻量，用 div + 内联样式手写更灵活，减少包体积。只用了 4 个基础组件：

| 组件 | 解决什么问题 |
|------|-------------|
| Avatar | 区分用户/AI 头像，不同颜色 |
| Input | 消息输入，回车发送，加载中禁用 |
| Button | 发送（loading 防重复）+ 复制（图标切换） |
| message | 全局提示，复制成功/失败反馈 |

**为什么用 Zustand 不用 Redux？**

Redux 需要写 action、reducer、dispatch，代码量大。Zustand 直接 `create()` 就能用，适合中小型项目。

---

## 总结

| 亮点 | 核心问题 | 解决方案 |
|------|---------|---------|
| 会话持久化 + 槽位管理 | 重启丢失、多轮信息收集、用户隔离 | MySQL 持久化 + 槽位合并写入 + 消息历史截断 + 会话归属校验 |
| 正则优先 + LLM 兜底 | LLM 慢、贵、不稳定；正则覆盖有限 | 正则匹配明确关键词 + LLM 兜底是常态 + LLM 只补充不覆盖 |
| 真流式 SSE 输出 | LLM 生成慢，用户等不及 | chatModel.stream() 逐 token 推送 + ReAct 循环 + 参数自动注入 |
| 打字机效果 | 推太快没感觉，推太慢不耐烦 | buffer + 定时器，每 20ms 取 2-3 字符 |
| SSE 流式消费 | EventSource 只支持 GET、半包问题 | fetch + ReadableStream + buffer 拼接 |
| 三路并行定位 | GPS 慢、可能失败 | GPS + IP + 浏览器三路并行，5 秒硬超时 |
| 登录自动清空 | 不同用户聊天记录混在一起 | Zustand subscribe 监听 token 变化，自动清空 |
| 主题水波纹 | 主题切换生硬 | View Transitions API + 点击坐标 CSS 变量 |
| 前端组件设计 | 轻量 UI 不需要重型组件 | 只用 4 个 antd 组件 + div 内联样式 |
