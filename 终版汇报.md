# 终版汇报 — 智能购票助手核心模块

> 汇报人：昀间
> 日期：2026-08-06

---

## 我负责的模块

我负责智能购票助手的 AI 对话链路，包括三个部分：**后端智能体记忆、工程化意图识别、前端对话页面**。

---

## 核心问题：前期准确度低，后期准确度高

### 前期问题

太依赖 LLM 的智能性，以为 LLM 能自己记住之前的信息、自己判断该调用什么工具、自己传对参数。结果 LLM 做不到，导致"查找不到但实际有信息"。

**具体表现**：
- 用户第一轮说"我想看教父"，第二轮说"明天下午"，LLM 回复"请问您想看什么电影"
- 用户说"附近有什么影院"，LLM 没传定位信息，返回"暂无场次信息"
- 用户说"帮我查一下我的订单"，LLM 没传用户 ID，返回"查不到"

### 后期解决

严格约束 LLM，不让 LLM 自己发挥：
- 槽位状态显式注入 System Prompt，LLM 必须看到
- 槽位齐全时强制注入工具调用指令，LLM 必须调用
- 参数自动注入，LLM 不需要自己传
- 意图识别正则优先，LLM 只兜底

**核心转变**：从"相信 LLM 能自己做好"变成"LLM 只能做有限的事，关键逻辑由后端控制"。

---

## 消息存储机制

消息同时保存在两个地方：

### 1. 数据库（MySQL）

用户发送消息后，立即保存到 MySQL 的 `hx_chat_sessions` 表：

```java
// ChatAgentService.java
saveMessage(sessionId, "user", content);

// 保存到数据库
private void saveMessage(String sessionId, String role, String content, List<Map<String, Object>> cards) {
    ChatSession session = chatSessionMapper.findBySessionId(sessionId);
    List<Map<String, Object>> messages = parseJson(session.getMessages(), List.class);
    messages.add(Map.of("role", role, "content", content, "timestamp", ...));
    chatSessionMapper.appendMessage(sessionId, MAPPER.writeValueAsString(messages));
}
```

### 2. Prompt（System Prompt + 消息历史）

每次请求时，从数据库加载历史消息，构建完整的上下文：

```java
// ChatAgentService.java
List<org.springframework.ai.chat.messages.Message> messages = new ArrayList<>();
messages.add(new SystemMessage(systemContent));  // System Prompt

// 从数据库加载历史消息
List<Map<String, Object>> history = parseJson(session.getMessages(), List.class);
if (history != null) {
    int maxHistory = 20;
    int startIndex = Math.max(0, history.size() - maxHistory);
    List<Map<String, Object>> trimmedHistory = history.subList(startIndex, history.size());
    for (Map<String, Object> msg : trimmedHistory) {
        messages.add(new UserMessage(msgContent));  // 加入 Prompt
    }
}
messages.add(new UserMessage(content));  // 当前消息
```

### 完整流程

```
用户发送消息
    │
    ▼
1. 保存到数据库（MySQL）
   saveMessage(sessionId, "user", content)
    │
    ▼
2. 从数据库加载历史消息
   parseJson(session.getMessages(), List.class)
    │
    ▼
3. 构建 Prompt
   System Prompt + 历史消息 + 当前消息
    │
    ▼
4. 发给 LLM
   chatModel.stream(prompt)
    │
    ▼
5. LLM 回复
    │
    ▼
6. 保存 AI 回复到数据库
   saveMessage(sessionId, "assistant", fullReply, cards)
```

### 关键点

- **数据库是持久化存储**：重启不丢失，60 分钟过期
- **Prompt 是临时构建**：每次请求时从数据库加载历史消息，构建完整的上下文
- **消息历史截断**：最多保留最近 20 条，防止超出 LLM 上下文窗口

---

## 提示词工程

### System Prompt 结构

System Prompt 是给 LLM 的"工作手册"，告诉它你是谁、能做什么、当前什么情况。

```java
// ChatAgentService.java L183-208
String systemContent = AgentPrompts.AGENT_SYSTEM_PROMPT;
systemContent += "\n\n当前用户意图: " + intent.name();

Map<String, Object> slots = parseJson(session.getSlots(), Map.class);
if (slots != null && !slots.isEmpty()) {
    systemContent += "\n当前槽位状态: " + slotsToString(slots);
}
if (city != null && !city.isBlank()) {
    systemContent += "\n用户当前定位城市: " + city;
}
if (lat != null && lng != null) {
    systemContent += "\n用户当前定位经纬度: lng=" + lng + ", lat=" + lat;
}
if (userId != null) {
    systemContent += "\n当前用户ID: " + userId;
}

// 槽位完整性预检
String forceToolHint = buildSlotCompletionHint(slots, intent);
if (forceToolHint != null) {
    systemContent += "\n\n" + forceToolHint;
}
```

### System Prompt 内容

System Prompt 包含以下部分：

1. **角色定义**：你是一个热爱电影的购票伙伴
2. **职责边界**：只负责理解和查询，不负责执行交易
3. **能力列表**：只能调用以下工具，绝对不能编造
4. **人情味交互准则**：有温度的伙伴，不是冷冰冰的客服
5. **行为准则**：购票流程、退票流程、价格查询等
6. **场次查询约束**：自动注入定位和时间参数
7. **时间参数强制规则**：必须传 date 和 time_preference
8. **槽位管理**：调用 save_slots 保存购票信息
9. **用户身份**：自动提供当前用户 ID
10. **城市感知**：自动提供用户定位城市
11. **常见问题参考**：退票政策、IMAX 厅、VIP 厅等
12. **输出格式**：纯文本，不要用 Markdown

### 动态注入

System Prompt 不是固定的，而是动态构建的：

- **当前用户意图**：正则识别或 LLM 兜底的结果
- **当前槽位状态**：电影名、影院名、时间、数量
- **用户当前定位城市**：GPS 或 IP 定位的结果
- **用户当前定位经纬度**：用于距离计算
- **当前用户 ID**：用于查询订单
- **槽位完整性预检**：槽位齐全时强制调用工具

### 槽位完整性预检

当电影+影院槽位齐全时，注入强制工具调用指令：

```java
// ChatAgentService.java L836-870
private String buildSlotCompletionHint(Map<String, Object> slots, IntentDetector.Intent intent) {
    if (slots == null || slots.isEmpty()) return null;
    if (intent != IntentDetector.Intent.BOOK_TICKET) return null;

    String movieName = (String) slots.get("movie_name");
    String cinemaName = (String) slots.get("cinema_name");

    if (movieName == null || movieName.isBlank()) return null;
    if (cinemaName == null || cinemaName.isBlank()) return null;

    StringBuilder hint = new StringBuilder();
    hint.append("## 重要：槽位已齐全，必须立即调用工具\n");
    hint.append("当前已收集到完整信息：电影=").append(movieName)
        .append("，影院=").append(cinemaName);
    hint.append("你必须在本轮回复中立即调用 search_sessions 工具查询场次，");
    hint.append("绝对不要追问用户任何信息，绝对不要只回复文字而不调用工具。");
    return hint.toString();
}
```

---

## 前端组件使用

### Chat 页面

**文件**：`src/pages/Chat/index.tsx`

用了 4 个 antd 组件：

| 组件 | 位置 | 用途 | 为什么用 |
|------|------|------|---------|
| **Avatar** | L219 | 区分用户/AI 头像 | 需要图标 + 背景色区分对话双方 |
| **Button** | L284, L350 | 发送按钮 + 复制按钮 | 需要 loading 状态防重复提交，需要图标切换 |
| **Input** | L335 | 消息输入框 | 需要 onPressEnter 回车发送，disabled 防止重复发送 |
| **message** | L51, L54 | 全局提示 | 复制成功/失败反馈 |

没用的重型组件：
- **Card**：卡片用 div + 内联样式手写，更灵活
- **List**：消息列表用 div + map 循环渲染
- **Spin**：加载动画用 CSS 动画（三个跳动圆点）
- **Modal**：没有弹窗需求
- **Form**：没有表单需求

### MainLayout

**文件**：`src/layouts/MainLayout.tsx`

用了 4 个 antd 组件：

| 组件 | 位置 | 用途 |
|------|------|------|
| **Input** | L200 | 搜索城市（Input.Search） |
| **message** | L53, L72, L74, L134 | 定位成功/失败、切换城市反馈 |
| **Popover** | L371 | 定位弹窗 |
| **Spin** | L170 | 定位中加载 |

### 为什么 Chat 页面只用 4 个组件

1. **更灵活**：div + 内联样式可以完全控制样式，不受组件限制
2. **减少包体积**：不引入重型组件，减少打包体积
3. **性能更好**：减少组件层级，提升渲染性能

---

## 总结

三个模块形成完整闭环：**意图识别**决定用户请求路由到哪个子 Agent，**智能体记忆**保证上下文连贯和用户偏好积累，**前端对话页面**提供流畅的用户交互。

技术上做了多层降级（正则→LLM→兜底）、异步沉淀（不阻塞响应）、遗忘机制（防止记忆膨胀）、打字机效果（buffer + 定时器逐字吐出），保证了系统的稳定性和用户体验。
