# 初学者快速上手指南 — 智能购票助手核心模块

> 这份文档帮助初学者快速理解项目中用到的技术栈和核心代码，不需要有 AI 或前端经验。

---

## 一、技术栈速览

### 前端（zzoneticket 仓库）

| 技术 | 是什么 | 在项目里干什么 |
|------|--------|---------------|
| **React** | 前端框架 | 构建页面，管理组件 |
| **Ant Design (antd)** | UI 组件库 | 提供 Avatar、Button、Input、message、Popover、Spin 等组件 |
| **@ant-design/icons** | 图标库 | 提供机器人、发送、复制、定位、主题切换等图标 |
| **Zustand** | 状态管理 | 管理消息列表、会话 ID、加载状态、位置、主题等全局数据 |
| **Bigfish** | 蚂蚁内部前端框架 | 提供路由导航（useNavigate）、请求封装（request） |
| **fetch + ReadableStream** | 浏览器原生 API | 消费 SSE 流式数据 |
| **axios** | HTTP 客户端 | 普通 API 请求（非 SSE），自动携带 JWT Token |
| **OneAPI** | 蚂蚁接口生成工具 | 自动生成 ChatController.ts 等接口文件 |

### 后端（zzvonehxtickettest 仓库）

| 技术 | 是什么 | 在项目里干什么 |
|------|--------|---------------|
| **SOFABoot** | 蚂蚁内部 Java 框架 | 提供 Web 服务，处理 HTTP 请求 |
| **Spring AI** | Spring 官方 AI 框架 | 调用 LLM，支持 Function Calling 和流式输出 |
| **SOFA AI** | 蚂蚁内部 AI 网关 | 兼容 OpenAI API，调用 DeepSeek 模型 |
| **MyBatis** | ORM 框架 | 操作 MySQL 数据库 |
| **MySQL** | 关系型数据库 | 存储会话、槽位、消息等数据 |

---

## 二、前后端分工

### 前端负责什么

前端（zzoneticket 仓库）是一个纯前端项目，**不包含任何 AI 逻辑**。它只负责：

1. **展示对话界面**：消息列表、卡片渲染、输入框
2. **SSE 流式消费**：接收后端推送的流式数据，逐段显示
3. **会话管理**：通过 `sessionId` 关联会话，调用后端接口查询/删除历史会话
4. **位置感知**：三路并行定位（GPS + IP + 浏览器），把位置信息传给后端
5. **主题切换**：深色/浅色主题，View Transitions API 水波纹动画
6. **认证管理**：JWT Token 存储，登录/登出，Token 过期自动跳转

### 后端负责什么

后端（zzvonehxtickettest 仓库）是 Java SOFABoot 项目，包含所有 AI 逻辑：

1. **智能体记忆**：会话持久化 + 槽位管理 + 消息历史
2. **意图识别**：正则优先 + LLM 兜底
3. **LLM 调用**：通过 SOFA AI 网关调用 DeepSeek，Spring AI 框架
4. **SSE 流式推送**：`chatModel.stream()` 真流式输出，逐 token 推送

### 前后端交互

```
前端                          后端
  │                             │
  │── POST /api/chat/send ─────→│  发送消息（带 sessionId + 位置信息）
  │                             │
  │←── SSE 流式推送 ────────────│  后端逐 token 推送 LLM 回复
  │                             │
  │── GET /api/chat/sessions ──→│  查询历史会话列表
  │                             │
  │── GET /api/chat/sessions/{id}/messages ──→│  加载某个会话的消息
  │                             │
  │── DELETE /api/chat/sessions/{id} ────────→│  删除某个会话
```

---

## 三、前端快速上手

### 1. 页面长什么样

```
┌─────────────────────────────────────────┐
│  智能马力电影  首页  电影  影院  [主题] [定位] [我的] │  ← 导航栏
├─────────────────────────────────────────┤
│      消息列表区域                         │
│  ┌─────┐                                │
│  │头像 │ 消息气泡                        │
│  └─────┘                                │
│  ┌─────┐                                │
│  │头像 │ 消息气泡                        │  ← AI 消息下方有"复制"按钮
│  └─────┘ [复制]                         │
│  ┌─────┐                                │
│  │头像 │ 电影卡片                        │  ← 卡片：海报+标题+评分
│  └─────┘                                │
├─────────────────────────────────────────┤
│  [输入框............] [发送]             │  ← 回车发送，加载中禁用
└─────────────────────────────────────────┘
```

### 2. 核心概念

**Zustand 状态管理**：全局的"数据仓库"，任何组件都能读写。项目用了 4 个 store：

```typescript
// 聊天状态：消息列表、会话 ID、加载状态
const { messages, isLoading, addMessage } = useChatStore();

// 位置状态：城市、经纬度
const { city, lat, lng } = useLocationStore();

// 认证状态：token、用户信息
const { token, user } = useAuthStore();

// 主题状态：深色/浅色
const { mode, toggleWithRipple } = useThemeStore();
```

**SSE 流式**：后端不是一次性返回完整回复，而是一段一段地推过来，前端逐段显示。

```typescript
const response = await fetch('/api/chat/send', { method: 'POST' });
const reader = response.body.getReader();
while (true) {
  const { done, value } = await reader.read();
  if (done) break;
  // 处理每一段数据
}
```

**打字机效果**：后端推过来的文字先存到 buffer，再用定时器逐字显示。

```typescript
// 每 20ms 从 buffer 取 2-3 个字符
const chunk = typingBuffer.substring(0, 3);
typingBuffer = typingBuffer.substring(chunk.length);
last.content += chunk;
```

**三路并行定位**：GPS + IP + 浏览器同时发起，5 秒硬超时，哪个先返回用哪个。

```typescript
// geolocation.ts
getCurrentPosition() → 三路并行
  ├── AMap.Geolocation（高德 GPS 定位）
  ├── AMap.CitySearch（高德 IP 定位）
  └── navigator.geolocation（浏览器原生定位）
```

**主题切换**：使用 View Transitions API 实现水波纹动画。

```typescript
// themeStore.ts
toggleWithRipple(cx, cy) {
  const vt = document.startViewTransition?.bind(document);
  if (vt) {
    const transition = vt(doSwitch);  // 浏览器自动做新旧快照
    transition?.finished?.finally(cleanup);
  } else {
    doSwitch();  // 不支持 VT 的浏览器直接切换
  }
}
```

**登录自动清空聊天记录**：新 token 出现时自动清空，避免不同用户的对话混在一起。

```typescript
// authStore.ts
useAuthStore.subscribe((state, prevState) => {
  if (!prevState.token && state.token) {
    useChatStore.getState().clearMessages();
  }
});
```

### 3. 代码文件说明

| 文件 | 作用 | 你需要知道什么 |
|------|------|---------------|
| `src/pages/Chat/index.tsx` | 对话页面 | 消息渲染、发送逻辑、卡片渲染 |
| `src/services/chat.ts` | SSE API 层 | fetch + ReadableStream 消费 SSE |
| `src/services/request.ts` | axios 封装 | 普通 API 请求，自动带 JWT Token，401 自动跳转登录 |
| `src/services/zzvonehxtickettest/ChatController.ts` | OneAPI 自动生成 | 会话管理接口（listSessions/getSessionMessages/deleteSession/sendMessage） |
| `src/stores/chatStore.ts` | 聊天状态 | 消息列表、打字机效果 |
| `src/stores/locationStore.ts` | 位置状态 | 城市、经纬度 |
| `src/stores/authStore.ts` | 认证状态 | JWT Token、登录/登出、Token 过期检查 |
| `src/stores/themeStore.ts` | 主题状态 | 深色/浅色切换、View Transitions 水波纹 |
| `src/utils/geolocation.ts` | 定位工具 | 三路并行定位（GPS + IP + 浏览器） |
| `src/utils/amap-search.ts` | 高德搜索 | 地点搜索、逆地理编码 |
| `src/layouts/MainLayout.tsx` | 主布局 | 导航栏、定位、主题切换、登录守卫 |
| `src/app.tsx` | 应用入口 | 主题配置、请求拦截器 |

---

## 四、后端快速上手

### 1. 智能体记忆

**问题**：LLM 的上下文窗口有限，对话长了 token 不够用；服务重启后会话状态丢失。

**解决**：会话持久化 + 槽位管理 + 消息历史截断。

**存储方案**：MySQL 一张表 `hx_chat_sessions`，所有数据用 TEXT 字段存 JSON：

```java
// ChatSession.java — 对话会话实体
public class ChatSession {
    private Long id;
    private String sessionId;    // 会话 ID
    private Integer userId;      // 用户 ID
    private String slots;        // 槽位状态（JSON 字符串）
    private String lastIntent;   // 最后一次意图
    private String context;      // 上下文（JSON 字符串）
    private String messages;     // 消息历史（JSON 数组）
    private LocalDateTime gmtExpire;  // 过期时间（60 分钟）
}
```

**记忆实现**：

```java
// ChatAgentService.java

// 1. 会话管理：获取或创建会话
private ChatSession getOrCreateSession(String sessionId, Long userId) {
    ChatSession session = chatSessionMapper.findBySessionId(sessionId);
    if (session != null) {
        // 校验会话归属：已存在的 session 必须属于当前用户
        if (!session.getUserId().equals(userId.intValue())) {
            return null;  // 不是自己的会话，拒绝访问
        }
        return session;
    }
    // 创建新会话
    session = new ChatSession();
    session.setSessionId(sessionId);
    session.setUserId(userId.intValue());
    session.setSlots("{}");
    session.setMessages("[]");
    session.setGmtExpire(LocalDateTime.now().plusMinutes(60));  // 60 分钟过期
    chatSessionMapper.insert(session);
    return session;
}

// 2. 消息保存：每条消息追加到 messages JSON 数组
private void saveMessage(String sessionId, String role, String content) {
    ChatSession session = chatSessionMapper.findBySessionId(sessionId);
    List<Map<String, Object>> messages = parseJson(session.getMessages(), List.class);
    messages.add(Map.of("role", role, "content", content, "timestamp", LocalDateTime.now().toString()));
    chatSessionMapper.appendMessage(sessionId, MAPPER.writeValueAsString(messages));
}

// 3. 槽位合并：新槽位合并到已有槽位，不是覆盖
private void mergeSlots(ChatSession session, Map<String, Object> extracted, String city) {
    Map<String, Object> existing = parseJson(session.getSlots(), Map.class);
    // 覆盖语义：movie_name 变 → 清 cinema_name + 派生槽位
    if (extracted.containsKey("movie_name") && !Objects.equals(
            extracted.get("movie_name"), existing.get("movie_name"))) {
        existing.remove("cinema_name");
        existing.remove("session_id");
    }
    existing.putAll(extracted);  // 合并，不是替换
    session.setSlots(MAPPER.writeValueAsString(existing));
    chatSessionMapper.updateSlotsAndContext(...);
}

// 4. 历史截断：最多保留最近 20 条消息（约 10 轮对话）
List<Map<String, Object>> history = parseJson(session.getMessages(), List.class);
int maxHistory = 20;
int startIndex = Math.max(0, history.size() - maxHistory);
List<Map<String, Object>> trimmedHistory = history.subList(startIndex, history.size());
```

**关键概念**：
- **槽位**：购票需要的信息（电影名、影院名、时间、数量），存在 MySQL `slots` 字段（JSON），多轮对话逐步收集
- **槽位合并**：`mergeSlots()` 先读已有槽位再 merge，不是覆盖。电影名变了会清空影院名（因为影院和电影是关联的）
- **历史截断**：最多保留最近 20 条消息，防止超出 LLM 上下文窗口
- **会话过期**：60 分钟过期，过期后自动创建新会话

### 2. 意图识别

**问题**：全部走 LLM 意图识别，每次调用消耗 token 且有延迟。

**解决**：正则优先 + LLM 兜底。

```java
// IntentDetector.java — 意图识别器

// 购票关键词
private static final Pattern BOOK_PATTERN = Pattern.compile(
    "(买票|订票|购票|订.*票|购.*票|买.*票|想看|要看|《.+》.*(?:买|订|看)|(?:想去|要去|去).+(?:影院|影城|电影院))"
);

// 退票关键词
private static final Pattern REFUND_PATTERN = Pattern.compile(
    "(退票|退款|退掉|退了|取消订单|不看了|不要了)"
);

// 查电影关键词
private static final Pattern MOVIE_PATTERN = Pattern.compile(
    "(什么电影|有什么电影|电影推荐|推荐.*电影|评分.*高|热映|正在.*映|好看.*电影)"
);

// 查影院关键词
private static final Pattern CINEMA_PATTERN = Pattern.compile(
    "(什么影院|有什么影院|附近.*影院|影院.*推荐|电影院|哪里.*看电影)"
);

// 查订单关键词
private static final Pattern ORDER_PATTERN = Pattern.compile(
    "(我的.*订单|订单.*列表|待支付|已购买|已支付.*订单|我的.*票|取票码)"
);

public Intent detect(String text) {
    // 打招呼最优先
    if (GREETING_PATTERN.matcher(text).find()) return Intent.REJECT;
    // 退票优先
    if (REFUND_PATTERN.matcher(text).find()) return Intent.REFUND_TICKET;
    // 查订单在购票之前（避免"我的票"被误判为购票）
    if (ORDER_PATTERN.matcher(text).find()) return Intent.QUERY_ORDER;
    if (MOVIE_PATTERN.matcher(text).find()) return Intent.QUERY_MOVIE;
    if (BOOK_PATTERN.matcher(text).find()) return Intent.BOOK_TICKET;
    if (CINEMA_PATTERN.matcher(text).find()) return Intent.QUERY_CINEMA;
    // 兜底：交给 LLM Function Calling 自行判断
    return Intent.QUERY_MOVIE;
}
```

**LLM 兜底**：正则不够自信时调用 LLM 补充意图和槽位。

```java
// ChatAgentService.java

// 条件1: 意图不明确
if (!isIntentConfident(content, intent) && !isGreeting(content)) {
    needLLM = true;
}
// 条件2: 消息明显包含信息但正则抽不到任何槽位
if (!needLLM && extracted.isEmpty() && content.length() > 5) {
    needLLM = true;
}
// 条件3: 消息含绝对日期但正则没抽到时间槽位
if (!needLLM && containsAbsoluteDate(content) && !extracted.containsKey("time_expression")) {
    needLLM = true;
}

if (needLLM) {
    LlmExtractionResult llmResult = llmExtract(content);
    // LLM 意图只在正则不够自信时覆盖
    if (llmResult.intent != null && !isIntentConfident(content, intent)) {
        intent = llmResult.intent;
    }
    // LLM 槽位只补充正则没抽到的，不覆盖正则已有的
    for (Map.Entry<String, Object> entry : llmResult.slots.entrySet()) {
        if (!extracted.containsKey(entry.getKey())) {
            extracted.put(entry.getKey(), entry.getValue());
        }
    }
}
```

**槽位抽取**（SlotManager.java）：

```java
// 电影名：优先匹配书名号《xxx》
private static final Pattern MOVIE_PATTERN = Pattern.compile("[《【「](.+?)[》】」]");

// 影院名：xx影院/影城/电影院
private static final Pattern CINEMA_PATTERN = Pattern.compile(
    "([\\u4e00-\\u9fa5a-zA-Z0-9]{2,10})(影院|影城|电影院|国际影城|环球影城)"
);

// 购票数量：N张/票
private static final Pattern COUNT_PATTERN = Pattern.compile("(\\d+)\\s*(?:张|票)");

// 时间表达：今天/明天/后天 + 上午/下午/晚上 + 时间点
private static final Pattern TIME_PATTERN = Pattern.compile(
    "(今天|明天|后天|大后天|周[一二三四五六日天])" +
    "(?:\\s*(上午|下午|晚上|中午))?" +
    "(?:\\s*(\\d{1,2})[:点](\\d{2})?)?"
);

// 绝对日期：7月15号 / 7月15号晚上8点
private static final Pattern ABSOLUTE_DATE_FULL_PATTERN = Pattern.compile(
    "(\\d{1,2})\\s*月\\s*(\\d{1,2})\\s*[号日]" +
    "(?:\\s*(上午|下午|晚上|中午))?" +
    "(?:\\s*(\\d{1,2})[:点](\\d{2})?)?"
);
```

### 3. SSE 流式输出

**真流式**：使用 Spring AI 的 `chatModel.stream()` 逐 token 推送。

```java
// ChatAgentService.java

chatModel.stream(prompt)
    .doOnNext(chunk -> {
        String delta = chunk.getResult().getOutput().getText();
        if (delta != null && !delta.isEmpty()) {
            textBuilder.append(delta);
            // 逐 token 推送给前端
            eventCallback.accept(Map.of("type", "text_delta", "content", delta));
        }
        // 工具调用也实时推送
        List<AssistantMessage.ToolCall> tcs = msg.getToolCalls();
        if (tcs != null) {
            for (AssistantMessage.ToolCall tc : tcs) {
                // 累积工具调用参数
            }
        }
    })
    .blockLast();
```

**Agent 循环（ReAct）**：

```java
for (int iteration = 0; iteration < MAX_ITERATIONS; iteration++) {
    // 调用 LLM
    chatModel.stream(prompt).doOnNext(chunk -> { ... }).blockLast();

    if (!tcBuilders.isEmpty()) {
        // 有工具调用：执行工具，回灌 messages，继续循环
        for (ToolCallBuilder tcb : tcBuilders.values()) {
            String result = callback.call(toolArgs);
            toolResponses.add(new ToolResponseMessage.ToolResponse(...));
        }
        messages.add(new ToolResponseMessage(toolResponses));
        continue;  // 继续下一轮迭代
    }

    // 没有工具调用：纯文本回复，结束循环
    String responseContent = textBuilder.toString().trim();
    if (!responseContent.isEmpty()) {
        fullReply = responseContent;
        break;
    }
}
```

---

## 五、怎么跑起来

### 前端

```bash
npm install
npm run dev
```

### 后端

```bash
# Java SOFABoot 项目
mvn spring-boot:run
```

---

## 六、常见问题

**Q: 为什么用 fetch 不用 EventSource？**
A: EventSource 只支持 GET 请求，但我们需要 POST 传 JSON body，所以用 fetch + ReadableStream。

**Q: 为什么用 Zustand 不用 Redux？**
A: Zustand 更轻量，不需要写 action/reducer/dispatch，直接 create() 就能用。

**Q: 打字机效果是怎么实现的？**
A: 后端推过来的文本先存到 buffer，再用 setInterval 每 20ms 取 2-3 个字符追加显示。

**Q: 后端是 Python 还是 Java？**
A: Java SOFABoot 项目，使用 Spring AI + SOFA AI 框架调用 DeepSeek。

**Q: 记忆是怎么实现的？**
A: MySQL 一张表 `hx_chat_sessions`，slots/messages 用 TEXT 存 JSON。槽位合并写入（不是覆盖），消息历史最多保留 20 条。

**Q: 意图识别为什么要正则优先？**
A: 为了平衡速度和准确率。高频意图用正则直接匹配（快），正则不够自信时才调 LLM（准）。

**Q: 前端有记忆逻辑吗？**
A: 没有。前端只负责通过 `sessionId` 关联会话，所有记忆逻辑（对话历史、槽位状态）都在后端。

**Q: ChatController.ts 是什么？**
A: OneAPI 自动生成的接口文件，定义了会话管理的 API 接口。不需要手动修改。

**Q: 定位是怎么做的？**
A: 三路并行：高德 GPS 定位 + 高德 IP 定位 + 浏览器原生定位，5 秒硬超时，哪个先返回用哪个。

**Q: 主题切换怎么做的？**
A: 用 View Transitions API 实现水波纹动画，不支持的浏览器直接切换。

**Q: SSE 是真流式还是模拟流式？**
A: 真流式。后端用 `chatModel.stream()` 逐 token 推送，前端用 ReadableStream 逐段接收。
