# 项目困难与解决 & 项目思考和收获

> 记录开发智能购票助手过程中遇到的困难、解决方案，以及项目带来的思考和收获。

---

## 一、遇到的困难和解决

### 困难一：LLM 上下文窗口不够用

**问题**：用户对话越来越长，每次都要把全部历史发给 LLM，很快超出 token 限制。

**解决**：
- 消息历史截断：最多保留最近 20 条消息（约 10 轮对话），超出部分直接截断
- 槽位持久化：把关键信息（电影名、影院、时间、数量）存到 MySQL，不依赖对话历史
- 会话过期：60 分钟自动过期，超时后创建新会话

**收获**：不是把所有数据都塞给 LLM，而是把关键信息结构化存储，需要时再注入。

---

### 困难二：LLM 意图识别不稳定

**问题**：LLM 可能把"想看个电影"中的"个"当作电影名，或者把"我的票"误判为购票意图。

**解决**：
- 正则优先：高频意图用正则直接匹配，不调 LLM，毫秒级响应
- LLM 兜底：正则不够自信时才调 LLM，且 LLM 只补充不覆盖
- 意图优先级：打招呼 > 退票 > 查订单 > 查电影 > 购票 > 查影院，避免误判
- 无效电影名黑名单：过滤"个""电影""一个"等量词

**收获**：不是所有问题都要交给 LLM，简单的规则匹配更快更准。LLM 只做正则覆盖不到的复杂场景。

---

### 困难三：SSE 流式输出的半包问题

**问题**：网络数据是流式到达的，一个 SSE 事件可能被拆成两次到达，直接解析会报错。

**解决**：
- buffer 暂存：用 buffer 暂存不完整的数据
- 按 `\n\n` 分割：SSE 事件以双换行分隔
- `events.pop()` 保留不完整部分，等下次读取时拼接

**收获**：网络编程中，数据到达是不保证完整的，必须用 buffer 处理半包。

---

### 困难四：打字机效果的速度控制

**问题**：后端推过来的文本如果直接显示，推太快没感觉，推太慢不耐烦。

**解决**：
- buffer + 定时器：文本先存到 buffer，每 20ms 取 2-3 个字符
- flushTyping()：SSE 流结束时等打字机吐完再设置 loading=false
- 切换会话时清空 buffer：防止旧会话的文字继续在新会话中显示

**收获**：用户体验的关键是"节奏感"，不是越快越好，也不是越慢越好，而是要模拟自然的打字速度。

---

### 困难五：多轮对话中槽位信息丢失

**问题**：用户第一轮说"我想看教父"，第二轮说"明天下午"。如果直接覆盖槽位，电影名就丢了。

**解决**：
- 槽位合并写入：`mergeSlots()` 先读已有槽位再 merge，不是覆盖
- 覆盖语义：电影名变了自动清空影院名（因为影院和电影是关联的）
- 槽位完整性预检：当电影+影院槽位齐全时，强制调用 search_sessions 查场次

**收获**：多轮对话中，信息收集是增量的，不是覆盖的。关联信息要一起清理。

---

### 困难六：定位慢且可能失败

**问题**：GPS 定位慢（可能需要几秒），可能失败（室内、权限拒绝），浏览器定位在 PC 上不可用。

**解决**：
- 三路并行：GPS + IP + 浏览器同时发起，哪个先返回用哪个
- 5 秒硬超时：超时后返回 null，由调用方决定兜底逻辑
- 定位失败兜底：默认城市"郑州东站"，保证应用可用

**收获**：不要串行等待，要并行尝试。任何可能失败的操作都要有兜底方案。

---

### 困难七：不同用户聊天记录混在一起

**问题**：用户 A 登录后聊了几句，退出登录。用户 B 登录后，看到了用户 A 的聊天记录。

**解决**：
- Zustand subscribe 监听 token 变化，新 token 出现时自动清空聊天记录
- 用动态 import 避免循环依赖
- 登录组件不需要关心 chatStore，完全解耦

**收获**：状态管理要考虑多用户场景，登录/登出时要清理敏感数据。

---

### 困难八：LLM 调用工具时缺少必要参数

**问题**：LLM 调用 `search_sessions` 时可能没传用户定位，调用 `get_user_orders` 时可能没传用户 ID。

**解决**：
- 后端自动注入：`search_sessions` / `search_cinemas` 注入用户经纬度
- `search_sessions` 注入槽位中的时间参数
- `get_user_orders` / `refund_order` / `cancel_order` 强制注入当前登录用户 ID

**收获**：不能完全信任 LLM 返回的参数，后端要做参数校验和自动注入。

---

## 二、项目思考和收获

### 1. 不是所有问题都要交给 LLM

LLM 很强大，但不是万能的。简单的规则匹配（正则）更快更准，成本更低。

**原则**：能用规则解决的不用 LLM，能用 LLM 解决的不用人工。

### 2. 用户体验的关键是"节奏感"

打字机效果不是越快越好，也不是越慢越好，而是要模拟自然的打字速度。每 20ms 取 2-3 个字符，在视觉上最接近自然打字。

**原则**：技术方案要服务于用户体验，不是为了炫技。

### 3. 任何可能失败的操作都要有兜底方案

定位可能失败、LLM 可能超时、网络可能中断、JSON 解析可能失败。每个环节都要有兜底方案。

**原则**：防御性编程，假设所有外部依赖都不可靠。

### 4. 状态管理要考虑多用户场景

登录/登出时要清理敏感数据，不同用户不能看到彼此的对话。

**原则**：状态管理要考虑边界情况，不是只考虑正常流程。

### 5. 不能完全信任 LLM 返回的参数

LLM 可能返回不完整的参数，后端要做参数校验和自动注入。

**原则**：LLM 是助手，不是权威。后端要做最后的把关。

### 6. 分层架构的重要性

意图识别分两层（正则 + LLM），记忆分三层（瞬时/中期/长期），错误处理分四层（HTTP/网络/业务/解析）。每一层都有明确的职责。

**原则**：复杂问题要分层解决，每一层只做一件事。

### 7. 技术选型要适合项目规模

Zustand 比 Redux 轻量，适合中小型项目。fetch + ReadableStream 比 EventSource 灵活，适合需要 POST 的场景。

**原则**：不是越流行的技术越好，而是越适合项目的技术越好。

---

## 三、总结

这个项目让我深入理解了：
- **LLM 应用开发**：不是简单地调用 API，而是要考虑上下文管理、意图识别、槽位收集、错误处理等
- **前后端协作**：前端负责展示和交互，后端负责 AI 逻辑和数据持久化
- **用户体验设计**：打字机效果、三路并行定位、主题水波纹，都是为了提升用户体验
- **防御性编程**：任何可能失败的操作都要有兜底方案
