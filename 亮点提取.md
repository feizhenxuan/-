# 核心亮点提取 — 解决问题的思考过程

> 这份文档不讲代码细节，而是讲**为什么这么做**——每个设计决策背后要解决的问题是什么。

---

## 亮点一：三级分层记忆架构

### 遇到的问题

LLM 的上下文窗口有限（通常 4K-128K token），但用户对话可能很长。如果每次都把全部历史塞给 LLM：
- 要么超出 token 限制，请求直接失败
- 要么 token 消耗太大，成本扛不住
- 服务重启后，之前的对话全丢了
- 用户上次说"喜欢科幻片"，下次来就忘了

### 思考过程

人类记忆分三层：瞬时记忆（几秒到几分钟）、短期记忆（几小时到几天）、长期记忆（永久）。我们参考这个模型，把 AI 的记忆也分三层：

| 层次 | 类比 | 解决什么问题 | 技术实现 |
|------|------|-------------|---------|
| 瞬时记忆 | 你正在想的事 | 当前对话的上下文窗口 | 内存 list，滑动窗口 + 压缩摘要 |
| 中期记忆 | 你今天还记得的事 | 服务重启后对话不丢失 | MySQL 持久化 |
| 长期记忆 | 你一直以来的偏好 | 跨会话的用户偏好积累 | ChromaDB 向量 + MySQL 三元组 |

### 关键设计决策

**1. 滑动窗口 + 压缩摘要**

问题：对话越来越长，token 不够用。

方案：不是简单地截断丢弃旧消息，而是把旧消息压缩成摘要放在最前面。LLM 仍能感知"之前聊了什么"，但只消耗少量 token。

```
压缩前：[user: 我想看教父] [assistant: 推荐教父] [user: 明天下午] ...（20条）
压缩后：[system: [对话摘要] 用户想看教父，时间明天下午...] [最近10条消息]
```

**2. 工具调用隔离**

问题：一次查询可能返回 20 家影院的数据，直接把上下文撑爆。

方案：工具返回的数据只保留前 500 字符，超出部分截断。LLM 只需要知道"查到了什么"，不需要看到完整数据。

**3. 槽位合并写入**

问题：多轮对话中，用户先说了电影名，后说了时间。如果直接覆盖，电影名就丢了。

方案：`update_slots` 先读已有槽位再 merge，不是覆盖。保证多轮对话中信息不丢失。

**4. 遗忘曲线衰减**

问题：记忆无限膨胀，系统越来越慢。

方案：每次对话后所有记忆 `weight *= 0.99`。不常用的记忆权重逐渐降低，常用的记忆通过重复出现保持高权重。模拟人类遗忘规律。

---

## 亮点二：四层意图识别策略

### 遇到的问题

用户意图多样："想看教父""推荐电影""第一个""明天下午""附近影院"……

如果全部走 LLM 意图识别：
- 每次调用消耗 token，成本高
- LLM 响应有延迟（1-3 秒），用户体验差
- LLM 返回结果不稳定，可能把"个"当作电影名

### 思考过程

分析用户请求分布后发现：**约 60% 的请求是高频意图**（购票、推荐、查影院），可以用关键词直接匹配，不需要调 LLM。

于是设计了四层策略，从快到慢逐级降级：

```
第1层：关键词匹配（不调LLM，毫秒级）
  → 覆盖 60% 请求，成本为 0
  ↓ 没匹配到
第2层：指代词解析（结合上下文）
  → 处理"第一个""最后一部"，不需要 LLM
  ↓ 没匹配到
第3层：LLM 意图识别（Function Calling，15秒超时）
  → 处理复杂意图，准确但慢
  ↓ LLM 没调工具
第4层：关键词兜底（最后防线）
  → LLM 失败了也能给出合理回复
```

### 关键设计决策

**1. 关键词路由优先**

问题：LLM 调用有延迟，高频意图每次都调 LLM 太慢。

方案：把"想看""推荐""好看吗"等高频意图做成关键词匹配，不调 LLM，毫秒级响应。

**2. 指代词解析**

问题：用户说"第一个"，LLM 不知道"第一个"指的是什么。

方案：从对话历史中提取上一轮工具返回的上下文（`[上下文-电影列表]`），解析出具体名称。还处理了超出范围的情况（列表只有 3 部，用户说"第五个"）。

**3. 无效电影名黑名单**

问题：LLM 可能把"想看个电影"中的"个"当作电影名。

方案：维护一个 23 个词的黑名单（"个""电影""一个""吗""吧"等），在 LLM 返回后过滤掉这些无效提取。

**4. 时间感知**

问题：用户说"早一点"，LLM 不知道"早一点"是什么意思。

方案：保留原日期，只调整时段。"早一点"→"上午"，"晚一点"→"晚上"。

---

## 亮点三：打字机效果

### 遇到的问题

后端 SSE 推过来的文本如果直接显示，会出现两种问题：
- 推得太快：一大段文字瞬间出现，用户没有"正在回复"的感觉
- 推得太慢：一个字一个字蹦，用户等得不耐烦

### 思考过程

观察 ChatGPT 的打字机效果：不是逐字显示，而是**每次显示 2-3 个字符**，速度自然流畅。

于是设计了一个 buffer + 定时器的方案：
1. 后端推过来的文本先存到 `typingBuffer`
2. 用 `setInterval` 每 20ms 从 buffer 取 2-3 个字符
3. 追加到最后一条消息上，触发 React 重渲染

### 关键设计决策

**1. 每次取 2-3 个字符**

问题：取 1 个太慢，取太多又不像打字。

方案：每次取 2-3 个字符，在视觉上最接近自然打字速度。

**2. flushTyping() 确保吐完再回调**

问题：SSE 流结束时，buffer 里可能还有没显示完的文字。如果直接设置 loading=false，用户会看到文字还没显示完 loading 就消失了。

方案：`flushTyping()` 把回调加入队列，等打字机吐完 buffer 再执行。

**3. 切换会话时清空 buffer**

问题：用户正在打字机效果中切换会话，旧会话的文字可能继续在新会话中显示。

方案：`clearMessages()` 和 `loadHistory()` 都会清空 typingBuffer 和定时器。

---

## 亮点四：SSE 流式消费

### 遇到的问题

需要实时接收后端推送的数据，但：
- EventSource 只支持 GET，我们需要 POST 传 JSON body
- 网络数据可能不完整到达（半包），直接解析会报错
- 用户可能中途取消请求

### 思考过程

**为什么用 fetch 不用 EventSource？**

EventSource 是浏览器原生的 SSE 客户端，但只支持 GET 请求。我们的接口需要 POST 传 JSON body（包含消息内容、会话 ID、位置信息），所以用 fetch + ReadableStream。

**半包问题怎么解决？**

网络数据是流式到达的，可能一个 SSE 事件被拆成两次到达。用 buffer 暂存不完整的数据，按 `\n\n` 分割事件，`events.pop()` 保留不完整部分，等下次读取时拼接。

### 关键设计决策

**1. buffer 半包拼接**

```typescript
buffer += decoder.decode(value, { stream: true });
const events = buffer.split('\n\n');
buffer = events.pop() || '';  // 不完整部分留到下次
```

**2. 30 秒超时 + AbortController**

```typescript
timeoutId = setTimeout(() => {
  controller.abort();
  callbacks.onError?.('回复超时，请重试');
}, 30000);
```

**3. 四层错误处理**

- HTTP 状态码错误（401 登录过期）
- 网络连接错误（AbortError 区分用户取消）
- 后端业务错误（data.type === 'error'）
- JSON 解析容错（解析失败静默跳过）

---

## 亮点五：三路并行定位

### 遇到的问题

需要获取用户位置来推荐附近影院，但：
- GPS 定位慢（可能需要几秒），用户等不及
- GPS 定位可能失败（室内、权限拒绝）
- 浏览器定位在 PC 上不可用

### 思考过程

传统方案是串行：先 GPS，失败了再 IP。但这样用户要等 GPS 超时才能看到结果。

我们改成**三路并行**：GPS + IP + 浏览器同时发起，哪个先返回用哪个，5 秒硬超时。

```
三路并行
  ├── 高德 GPS 定位（AMap.Geolocation）
  ├── 高德 IP 定位（AMap.CitySearch）
  └── 浏览器原生定位（navigator.geolocation）
  → 哪个先返回用哪个，5 秒硬超时
```

### 关键设计决策

**1. 三路并行而不是串行**

问题：串行定位慢，GPS 超时后才能试 IP。

方案：三路同时发起，哪个先返回用哪个，大幅提升定位速度。

**2. 5 秒硬超时**

问题：如果三路都失败，不能一直等。

方案：5 秒硬超时，超时后返回 null，由调用方决定兜底逻辑（默认城市）。

**3. 定位失败兜底**

问题：定位失败时不能让用户卡在空白页。

方案：默认城市"郑州东站"，保证应用可用。

---

## 亮点六：登录自动清空聊天记录

### 遇到的问题

用户 A 登录后聊了几句，退出登录。用户 B 登录后，看到了用户 A 的聊天记录。

### 思考过程

需要在登录/登出时清空聊天记录。但登录组件不应该直接操作 chatStore，这样会形成耦合。

### 关键设计决策

**使用 Zustand 的 subscribe 机制解耦**：

```typescript
// authStore.ts
useAuthStore.subscribe((state, prevState) => {
  if (!prevState.token && state.token) {
    // 新 token 出现（登录成功）→ 自动清空聊天记录
    import('./chatStore').then(({ useChatStore }) => {
      useChatStore.getState().clearMessages();
    });
  }
});
```

- 用 `subscribe` 监听 token 变化
- 用动态 `import` 避免循环依赖
- 登录组件不需要关心 chatStore，完全解耦

---

## 亮点七：主题切换水波纹动画

### 遇到的问题

主题切换时页面突然变色，用户体验生硬。

### 思考过程

现代浏览器支持 View Transitions API，可以自动做新旧快照的过渡动画。利用这个 API，可以实现以点击位置为圆心的水波纹扩散效果。

### 关键设计决策

**1. View Transitions API**

```typescript
const vt = document.startViewTransition?.bind(document);
if (vt) {
  const transition = vt(doSwitch);  // 浏览器自动做新旧快照
  transition?.finished?.finally(cleanup);
} else {
  doSwitch();  // 不支持的浏览器直接切换
}
```

**2. 点击坐标写入 CSS 变量**

```typescript
root.style.setProperty('--ripple-x', `${cx}px`);
root.style.setProperty('--ripple-y', `${cy}px`);
```

**3. 降级方案**

不支持 View Transitions API 的浏览器直接切换，不影响功能。

---

## 亮点八：前端组件设计

### 遇到的问题

- 需要区分用户和 AI 的消息
- 需要防止用户重复发送
- 需要支持一键复制 AI 回复
- 需要渲染多种类型的卡片（电影/影院/场次/订单）

### 思考过程

**为什么只用 4 个 antd 组件？**

antd 提供了很多重型组件（Card、List、Spin、Modal），但对话页面的 UI 比较轻量，用 div + 内联样式手写更灵活，减少包体积。只用了 4 个基础组件：

| 组件 | 解决什么问题 |
|------|-------------|
| Avatar | 区分用户/AI 头像，不同颜色 |
| Input | 消息输入，回车发送，加载中禁用 |
| Button | 发送（loading 防重复）+ 复制（图标切换） |
| message | 全局提示，复制成功/失败反馈 |

**为什么用 Zustand 不用 Redux？**

Redux 需要写 action、reducer、dispatch，代码量大。Zustand 直接 `create()` 就能用，适合中小型项目。

---

## 总结

| 亮点 | 核心问题 | 解决方案 |
|------|---------|---------|
| 三级分层记忆 | token 不够用、重启丢失、偏好遗忘 | 滑动窗口 + MySQL 持久化 + 向量/图谱 + 遗忘曲线 |
| 四层意图识别 | LLM 慢、贵、不稳定 | 关键词→指代词→LLM→兜底，逐级降级 |
| 打字机效果 | 推太快没感觉，推太慢不耐烦 | buffer + 定时器，每 20ms 取 2-3 字符 |
| SSE 流式消费 | EventSource 只支持 GET、半包问题 | fetch + ReadableStream + buffer 拼接 |
| 三路并行定位 | GPS 慢、可能失败 | GPS + IP + 浏览器三路并行，5 秒硬超时 |
| 登录自动清空 | 不同用户聊天记录混在一起 | Zustand subscribe 监听 token 变化，自动清空 |
| 主题水波纹 | 主题切换生硬 | View Transitions API + 点击坐标 CSS 变量 |
| 前端组件设计 | 轻量 UI 不需要重型组件 | 只用 4 个 antd 组件 + div 内联样式 |
